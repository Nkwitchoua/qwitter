const INIT_STATE = {
    chatsLoading: true,
    chatsLoaded: false,
    chats: [],
}

export default (state = INIT_STATE, action) => {
    switch(action.type) {



        default:
            return state;
    }
}