const INIT_STATE = {
    chatLoading: true,
    chatLoaded: false,
    currentChat: ""
}

export default (state = INIT_STATE, action) => {
    
    switch(action.type) {



        default:
            return state;
    }
}